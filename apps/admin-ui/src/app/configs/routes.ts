export enum Route {
  ADMIN = 'admin',
  LOGIN = '/admin/login',
  USERS = '/admin/users',
  USER_PROFILE = '/admin/users/:id',
  PRODUCTS = '/admin/products',
  PRODUCT_PROFILE = '/admin/products/:id',
}
